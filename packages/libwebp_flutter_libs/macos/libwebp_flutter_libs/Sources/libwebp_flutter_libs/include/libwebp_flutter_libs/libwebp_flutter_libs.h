#ifndef LIBWEBP_FLUTTER_LIBS_MACOS_H
#define LIBWEBP_FLUTTER_LIBS_MACOS_H

// Umbrella header for the libwebp_flutter_libs SPM target (macOS). Intentionally
// empty — all symbols consumed by Dart FFI come from the transitive libwebp
// dependency (SDWebImage/libwebp-Xcode), statically linked into the Runner binary.

#endif /* LIBWEBP_FLUTTER_LIBS_MACOS_H */
