name: libwebp
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  ffi: ^2.1.2
  logging: ^1.2.0
  meta: ^1.11.0

dev_dependencies:
  flutter_lints: ^3.0.0
  ffigen: ^11.0.0
  test: ^1.24.9
