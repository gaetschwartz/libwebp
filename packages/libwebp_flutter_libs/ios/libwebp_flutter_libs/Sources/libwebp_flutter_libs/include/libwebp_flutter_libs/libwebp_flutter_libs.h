#ifndef LIBWEBP_FLUTTER_LIBS_H
#define LIBWEBP_FLUTTER_LIBS_H

// Umbrella header for the libwebp_flutter_libs SPM target. Intentionally empty —
// all symbols consumed by Dart FFI come from the transitive libwebp dependency
// (SDWebImage/libwebp-Xcode), which is re-exported via static linking into the
// Runner binary.

#endif /* LIBWEBP_FLUTTER_LIBS_H */
